apiVersion: v1
kind: Service
metadata:
  name: employee-directory-service
spec:
  type: NodePort
  selector:
    app: employee-directory-app
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30008